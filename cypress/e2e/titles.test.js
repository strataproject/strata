/* eslint-env jest */
/* global cy */

/// <reference types="Cypress" />
describe('Pages have titles', () => {
  // pages.forEach(page => {
  //   const url = parse(page).name
  //   it(`${url} has a title`, () => {
  //     cy.visit(`/${url}`)
  //     expect(cy.title()).not.to.be.undefined
  //   })
  // })
})
