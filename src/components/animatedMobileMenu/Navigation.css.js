import styled from 'styled-components'
import { motion } from 'framer-motion'

export const StyledNavigation = styled(motion.ul)`
  list-style-type: none;
  margin: 0px;
  position: absolute;
  top: 40px;
  right: 40px;
  z-index: 1000;
`
